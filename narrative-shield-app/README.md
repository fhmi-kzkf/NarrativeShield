# NarrativeAegis™ — App Frontend

This folder contains the Next.js frontend and API route code for **NarrativeAegis™**.

For full setup documentation, features list, and hackathon details, please refer to the main repository [README.md](../README.md).

## Quick Start

1. Set up your `.env.local` file:
   ```env
   GOOGLE_GEMINI_API_KEY=your_gemini_api_key
   BRIGHT_DATA_API_KEY=your_bright_data_api_key
   ```
2. Run `npm install` to install dependencies.
3. Run `npm run dev` to start the local development server at `http://localhost:3000`.
